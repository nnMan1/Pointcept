
services:
  pointcept:
    image: "registry.gitlab.com/pmf5/pmf_ai/computer_vision/3d/3d_deep_learning_models:multiview" 
    tty: true
    stdin_open: true
    restart: unless-stopped
    shm_size: '178gb'
    memswap_limit: 121g
    build: 
      context: .
      dockerfile: Dockerfile
      args:
        - TORCH_VERSION=${TORCH_VERSION}
        - CUDA_VERSION=${CUDA_VERSION}
        - CUDNN_VERSION=${CUDNN_VERSION}
    environment:
      DISPLAY: $DISPLAY
    network_mode: "host"
    volumes:
      - .:/home
      - /tmp/.X11-unix:/tmp\.X11-unix
      - ${DATA_PATH}:/home/data
    deploy:
      resources:
        limits:
          memory: 120G
        #  cpus: '0.8'
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
